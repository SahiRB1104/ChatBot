# ChatBot

## Run the app

1. Install the Python dependencies:

```bash
pip install -r backend/requirement.txt
```

2. Start Flask. Streamlit will launch automatically from the Flask app:

```bash
python backend/app.py
```

You can also use `flask --app backend.app run`. The root route redirects to the Streamlit UI on port 8501.
